Unit SelfHost.Examples.LexDemo;

Get Console By IO.Console;

Static Function int Main(string args[]) {
    int value = 42;
    string name = "kinal";
    If (value > 0 && name != "") {
        Console.PrintLine(name);
    }
    Return 0;
}

