# 测试指南

## 运行测试

```bash
# 运行全部测试
python x.py test

# 运行单个测试
python x.py test tests/arrays.kn
```

## 测试文件

- 位于 `tests/` 目录
- 文件扩展名 `.kn`
- 以 `error_` 开头的文件是**负面测试**，预期编译失败并检查错误信息

## 编写测试

1. 在 `tests/` 下创建 `.kn` 文件
2. 正面测试：编写正常程序，预期编译并运行成功
3. 负面测试：文件名以 `error_` 开头，在文件内标注预期的错误信息
