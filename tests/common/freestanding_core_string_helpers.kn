Unit Tests.Freestanding.CoreStringHelpers;

Trusted Static Function void KMain(byte*bootInfo)
{
    string header= "boot info: ";
    string addr=[string](bootInfo);
    string line= header+ addr;
    int number= 42;
    string text= number.ToString();
    bool ready= true;
    string status= ready.ToString();

    If (line.Length()== 0|| text.Length()== 0|| status.Length()== 0)
    {
    }

    While (true)
    {
    }
}
